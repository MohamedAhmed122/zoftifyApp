import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    marginHorizontal: 16,
    marginBottom: 40
  },
  contentContainerStyle: {
    paddingBottom: 80,
  },
});
